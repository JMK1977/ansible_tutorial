---

- hosts: all
  become: true
  tasks:

  - name: remove packages from servers
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
        - "{{ mariadb_package }}"
        - "{{ samba_package }}"
      state: absent
